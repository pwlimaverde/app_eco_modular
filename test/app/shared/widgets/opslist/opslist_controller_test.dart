import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:eco_web_mobx/app/shared/widgets/opslist/opslist_controller.dart';
import 'package:eco_web_mobx/app/app_module.dart';

void main() {
  initModule(AppModule());
  // OpslistController opslist;
  //
  setUp(() {
    //     opslist = AppModule.to.get<OpslistController>();
  });

  group('OpslistController Test', () {
    //   test("First Test", () {
    //     expect(opslist, isInstanceOf<OpslistController>());
    //   });

    //   test("Set Value", () {
    //     expect(opslist.value, equals(0));
    //     opslist.increment();
    //     expect(opslist.value, equals(1));
    //   });
  });
}
